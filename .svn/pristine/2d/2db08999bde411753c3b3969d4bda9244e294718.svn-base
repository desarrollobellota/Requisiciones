import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AprobOrdenesComponent } from './aprob-ordenes.component';

describe('AprobOrdenesComponent', () => {
  let component: AprobOrdenesComponent;
  let fixture: ComponentFixture<AprobOrdenesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AprobOrdenesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AprobOrdenesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
